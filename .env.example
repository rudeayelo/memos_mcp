# Example environment configuration for Memos MCP Server

# ===========================================
# Upstream Memos Instance
# ===========================================

# Base URL of your Memos instance
# Default: http://localhost:5230
MEMOS_BASE_URL=http://localhost:5230

# API authentication token for Memos
# To get a token:
# 1. Log into your Memos instance
# 2. Go to Settings → Access Tokens
# 3. Create a new access token
# 4. Copy and paste it here
MEMOS_API_TOKEN=your-memos-token-here

# ===========================================
# OAuth 2.0 Authentication
# ===========================================

# Password for OAuth authorization (single-user authentication)
# This is the password you'll enter when authorizing Claude to access your Memos
OAUTH_PASSWORD=your-secure-password-here

# Public URL of this MCP server (used in OAuth metadata)
# Must be the externally-accessible HTTPS URL
# Example: https://memos-mcp.your-domain.com
OAUTH_ISSUER_URL=https://your-server-url.com

# Access token expiry time in seconds (default: 3600 = 1 hour)
OAUTH_TOKEN_EXPIRY_SECONDS=3600
